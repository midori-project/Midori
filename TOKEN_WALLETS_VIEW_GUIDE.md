# 🪙 Token Wallets View - User Guide

## 📋 Overview

หน้าสำหรับให้ผู้ใช้ดูรายละเอียด Token แต่ละประเภทที่ตัวเองมี (STANDARD, PREMIUM, BONUS, TRIAL)

## 🎯 Features

### 1. **Token Wallets Display**
- แสดง Token แต่ละประเภท (wallet) พร้อมยอดคงเหลือ
- แสดงเปอร์เซ็นต์ของยอดรวม
- Progress bar แสดงสัดส่วน
- สีและ icon แยกตามประเภท wallet

### 2. **Total Balance**
- แสดงยอด Token รวมทั้งหมด
- สถานะการใช้งาน (สามารถสร้างโปรเจคได้หรือไม่)
- แสดงลำดับการใช้งาน Token อัตโนมัติ

### 3. **Transaction History**
- ประวัติการใช้งาน Token ย้อนหลัง
- แสดงรายละเอียด: วันที่, ประเภท, จำนวน
- แยกสีเขียว (เพิ่ม) / แดง (ลด)

## 📁 Files Created

```
src/
├── app/(app)/(billing)/my-tokens/
│   └── page.tsx                              # My Tokens Page
└── components/
    └── tokens/
        └── TokenWalletsView.tsx               # Token Wallets View Component
```

## 🎨 UI Design

### Wallet Types & Colors

| Type | Icon | Color | Description |
|------|------|-------|-------------|
| **STANDARD** | 🆓 | Blue | Free tokens (รีเซ็ตทุกวัน) |
| **PREMIUM** | 💎 | Purple | Paid tokens (ซื้อมา) |
| **BONUS** | 🎁 | Yellow | Bonus tokens (โปรโมชั่น) |
| **TRIAL** | 🎫 | Green | Trial tokens (ทดลอง) |

### Usage Priority Order

```
1. TRIAL → ใช้ก่อน (หมดอายุเร็วสุด)
2. STANDARD → Free tokens (รีเซ็ตทุกวัน)
3. BONUS → Bonus tokens (โปรโมชั่น)
4. PREMIUM → Paid tokens (ใช้หลังสุด)
```

## 🚀 Access

### URL
```
/my-tokens
```

### Navigation
- จาก Navbar: คลิก "My Tokens"
- จาก Dashboard: คลิก "View Tokens"
- Direct Link: `/my-tokens`

## 📊 Component Details

### TokenWalletsView Component

**Props:**
```typescript
interface TokenWalletsViewProps {
  onRefresh?: () => void;
}
```

**Features:**
- ✅ โหลดข้อมูล wallets จาก `/api/billing/wallets`
- ✅ แสดง Total Balance
- ✅ แสดงรายละเอียดแต่ละ wallet
- ✅ Progress bar แสดงสัดส่วน
- ✅ ข้อมูลการหมดอายุ
- ✅ ข้อมูลการรีเซ็ต (สำหรับ STANDARD)
- ✅ ลำดับการใช้งาน

### My Tokens Page

**Features:**
- ✅ Token Wallets View
- ✅ Transaction History (30 รายการล่าสุด)
- ✅ Navigation buttons (เติม Token, Dashboard)
- ✅ Responsive design

## 🔧 API Endpoints Used

### GET /api/billing/wallets
ดึงข้อมูล Token wallets ของผู้ใช้ปัจจุบัน

**Response:**
```json
{
  "success": true,
  "data": {
    "totalBalance": 60,
    "wallets": [
      {
        "id": "wallet-1",
        "walletType": "STANDARD",
        "balanceTokens": 5,
        "lastTokenReset": "2024-01-01T00:00:00Z",
        "isActive": true,
        "expiresAt": null
      },
      {
        "id": "wallet-2",
        "walletType": "PREMIUM",
        "balanceTokens": 55,
        "lastTokenReset": null,
        "isActive": true,
        "expiresAt": null
      }
    ],
    "canCreateProject": true,
    "requiredTokens": 1
  }
}
```

### GET /api/billing/transactions
ดึงประวัติการใช้งาน Token

**Query Params:**
- `limit`: จำนวนรายการ (default: 50)

**Response:**
```json
{
  "success": true,
  "data": [
    {
      "id": "tx-1",
      "amount": -1.5,
      "type": "PROJECT_CREATION",
      "description": "Project creation",
      "createdAt": "2024-01-15T10:30:00Z"
    }
  ]
}
```

## 🎯 User Experience

### View Token Details
1. User คลิก "My Tokens" จาก Navbar
2. หน้าแสดง Total Balance และรายละเอียดแต่ละ wallet
3. เห็นได้ว่า:
   - มี Free tokens เท่าไหร่
   - มี Paid tokens เท่าไหร่
   - ใช้แล้วกี่เปอร์เซ็นต์
   - จะใช้ wallet ไหนก่อน

### Check Balance
- เห็นยอดรวม Token ทันที
- ตรวจสอบว่าเพียงพอสำหรับการสร้างโปรเจคหรือไม่
- ดูว่ามี wallet ไหนบ้างที่ใช้งานได้

### Monitor Usage
- ดู Transaction History ย้อนหลัง
- ตรวจสอบการใช้งาน Token แบบละเอียด
- ติดตามการได้รับ/ใช้ Token

## 💡 Benefits

### For Users
- ✅ **Transparency** - เห็นรายละเอียด Token ทุกประเภท
- ✅ **Control** - รู้ว่าเหลือ Token เท่าไหร่
- ✅ **Tracking** - ติดตามการใช้ Token
- ✅ **Understanding** - เข้าใจระบบลำดับการใช้งาน

### For Business
- ✅ **User Trust** - ผู้ใช้เห็นว่าได้ค่าที่จ่ายไป
- ✅ **Engagement** - ผู้ใช้เข้าไปดู Token บ่อยๆ
- ✅ **Retention** - ผู้ใช้เข้าใจระบบมากขึ้น
- ✅ **Monetization** - ผู้ใช้เห็นว่า Free tokens หมดแล้ว

## 🧪 Testing

### Manual Testing

1. **View Wallet Details:**
   - Login → ไปที่ `/my-tokens`
   - ตรวจสอบว่าเห็น Total Balance
   - ตรวจสอบว่าเห็น wallets ทั้งหมด

2. **Check Priority:**
   - ดูลำดับ wallet (TRIAL → STANDARD → BONUS → PREMIUM)
   - ตรวจสอบว่าลำดับถูกต้อง

3. **Transaction History:**
   - ดูประวัติการใช้งาน
   - ตรวจสอบว่าแสดงรายละเอียดถูกต้อง
   - ตรวจสอบว่าแยกสีถูกต้อง

4. **Responsive Design:**
   - ทดสอบบน mobile
   - ทดสอบบน tablet
   - ทดสอบบน desktop

## 🎨 Design Elements

### Wallet Cards
- Border สีตามประเภท
- Background สีอ่อน
- Icon และ label ชัดเจน
- Progress bar แสดงสัดส่วน
- ข้อมูลการหมดอายุ (ถ้ามี)

### Total Balance
- Gradient background (green)
- ตัวเลขขนาดใหญ่
- สถานะการใช้งาน
- ลำดับการใช้งาน

### Transaction Items
- Icon แยกตามประเภท
- สีเขียว (เพิ่ม) / แดง (ลด)
- วันที่และเวลา
- คำอธิบาย

## 📝 Summary

**My Tokens Page** เป็นหน้าสำคัญที่ให้ผู้ใช้:
- ดูรายละเอียด Token ทั้งหมด
- เข้าใจระบบลำดับการใช้งาน
- ติดตามการใช้ Token
- เชื่อมั่นในความโปร่งใสของระบบ

ระบบนี้จะช่วยสร้างความมั่นใจให้กับผู้ใช้ และช่วยให้การขาย Token เป็นไปอย่างราบรื่น! 🚀

