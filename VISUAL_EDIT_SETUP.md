# 🎨 Visual Edit Mode - Setup Guide

## ⚠️ สำคัญมาก: Project ต้อง Regenerate!

Visual Edit Mode script ถูก **embed ใน index.html template** แล้ว แต่ project ที่สร้างก่อนหน้านี้ยังไม่มี script

---

## 🚀 วิธี Setup (เลือก 1 วิธี)

### วิธีที่ 1: Regenerate Project เดิม ⭐ (แนะนำ)

1. **เปิด Project ที่ต้องการ**
2. **ไปที่ Chat/Edit** 
3. **พิมพ์คำสั่งแก้ไขเล็กน้อย** เช่น:
   ```
   เปลี่ยนสี hero เป็นสีน้ำเงิน
   ```
   หรือ
   ```
   เปลี่ยน heading เป็น "ยินดีต้อนรับสู่ร้านของเรา"
   ```
4. **รอระบบ generate เสร็จ**
5. **Refresh Preview** (F5)
6. **ทดสอบ Visual Edit Mode**

---

### วิธีที่ 2: สร้าง Project ใหม่

1. **ไปหน้า Projects**
2. **คลิก "Create New Project"**
3. **เลือก Business Category** (เช่น Restaurant, E-commerce)
4. **ใส่ keywords** และ **Generate**
5. **รอจนเสร็จ**
6. **ทดสอบ Visual Edit Mode**

---

## ✅ วิธีตรวจสอบว่า Project มี Visual Edit Script

### วิธีที่ 1: ดู Console
1. เปิด Preview
2. เปิด Console (F12)
3. ควรเห็น logs:
   ```
   🎨 Visual Edit script loaded (embedded)
   ✅ Visual Edit script initialized (embedded)
   ```

### วิธีที่ 2: ดู HTML Source
1. เปิด Preview
2. Right-click → **View Page Source**
3. กด Ctrl+F ค้นหา: `Visual Edit script`
4. **ถ้าเจอ** → มี script ✅
5. **ถ้าไม่เจอ** → ต้อง regenerate ❌

---

## 🎮 วิธีใช้ Visual Edit Mode

### 1. เปิด Edit Mode
- กด **Alt + E** (keyboard shortcut)
- หรือคลิกปุ่ม **"👁️ Preview"** ใน toolbar

### 2. แก้ไข Content
- **Hover** บน text/image → เห็น blue outline
- **Click** บน element → panel ทางขวาปรากฏ
- **แก้ไข** ข้อความหรือ URL
- กด **Save** (หรือ Ctrl/Cmd + Enter)

### 3. ปิด Edit Mode
- กด **Alt + E** อีกครั้ง
- หรือคลิกปุ่ม **"✏️ Edit Mode"**

---

## 🔍 Troubleshooting

### ปัญหา: กด Alt + E แล้วไม่เห็น outline

**วิธีแก้:**
1. เปิด Console ดู logs
2. ถ้าไม่เห็น "Visual Edit script loaded" → regenerate project
3. ถ้าเห็น แต่ไม่เห็น "🔒 Page frozen" → ลอง refresh preview

### ปัญหา: Click แล้ว Panel ไม่ขึ้น

**วิธีแก้:**
1. ตรวจสอบว่าเข้า Edit Mode แล้วหรือยัง (ดูปุ่มว่าเป็น "✏️ Edit Mode")
2. ดู Console ว่ามี log "🎯 Element clicked" หรือไม่
3. ถ้าไม่มี → element อาจไม่มี data-editable attribute

### ปัญหา: Hover แล้วไม่เห็นอะไร

**วิธีแก้:**
1. ตรวจสอบว่าเข้า Edit Mode แล้ว
2. Inspect element ดูว่ามี `data-editable="true"` หรือไม่
3. ถ้าไม่มี → regenerate project

---

## 📋 Quick Checklist

- [ ] Project ถูก regenerate หลังจากติดตั้ง Visual Edit (วันที่ 15 ต.ค. 2025)
- [ ] เปิด Console เห็น log "Visual Edit script loaded"
- [ ] กด Alt + E เห็น log "Page frozen"
- [ ] Hover บน text เห็น blue outline
- [ ] Click บน text เห็น panel ขวามือ
- [ ] แก้ไขและบันทึกได้

---

## 💡 Tips

1. **Project เก่าทั้งหมดต้อง regenerate** เพื่อให้มี Visual Edit Mode
2. **กด Alt + E** เร็วกว่าคลิกปุ่ม
3. **กด Esc** เพื่อปิด edit panel
4. **Ctrl/Cmd + Enter** เพื่อ save อย่างรวดเร็ว
5. **Hard Refresh** (Ctrl + Shift + R) ถ้าแก้แล้วไม่เปลี่ยน

---

**Created:** 15 ตุลาคม 2025  
**Status:** Ready to use 🚀

