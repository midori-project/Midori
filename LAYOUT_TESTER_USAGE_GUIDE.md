# 🎨 Layout Tester - คู่มือการใช้งาน

## ภาพรวม

Layout Tester เป็นเครื่องมือทดสอบและพรีวิว Variants ทุกประเภทในระบบ Template System พร้อมฟีเจอร์ครบครัน:

✅ **รองรับ Variants ทุกประเภท**: Hero, Footer, About, Menu  
✅ **Responsive Preview**: ดูแบบ Mobile, Tablet, Desktop, Full Width  
✅ **Mock Data Editor**: แก้ไขข้อมูลทดสอบแบบ Real-time  
✅ **Compare Mode**: เปรียบเทียบหลาย Variants พร้อมกัน  
✅ **Export Code**: คัดลอก Template Code ได้ทันที  
✅ **แก้ไขปัญหา Tailwind Dynamic Classes**: ใช้ Color Mapper แปลงเป็น inline styles

---

## 🚀 การเข้าถึง

เปิดหน้า Layout Tester ที่:
```
/layout-tester
```

---

## 📦 โครงสร้างหน้าจอ

```
┌─────────────────────────────────────────────────────────┐
│ 🎨 Advanced Variant System Tester                      │
│                                          [⚖️ Compare] [✏️ Edit Data] │
├─────────────────────────────────────────────────────────┤
│ 📦 Block Type:  [🎯 Hero] [📄 Footer] [📖 About] [🍽️ Menu]│
├─────────────────────────────────────────────────────────┤
│ 🏢 Business Category:  [Restaurant] [Cafe] [Salon]...  │
│ 🎭 Variants:  [Hero Stats] [Hero Split] [Hero Full]... │
├─────────────────────────────────────────────────────────┤
│ Info Bar + Controls:                                    │
│ Viewport: [📱📲🖥️📺]  Mode: [🖥️Live 💻Code]  [📋Copy]   │
└─────────────────────────────────────────────────────────┘
┌──────────┬──────────────────────────────────────────────┐
│ ✏️ Mock  │  Preview Area                                │
│ Data     │                                              │
│ Editor   │  [Variant Preview หรือ Compare View]        │
│ (Opt.)   │                                              │
└──────────┴──────────────────────────────────────────────┘
```

---

## 🎯 ฟีเจอร์หลัก

### 1. **Block Type Selector** 📦

เลือกประเภทของ Block ที่ต้องการทดสอบ:

- **🎯 Hero Sections**: Hero variants 5 แบบ (Stats, Split, Fullscreen, Minimal, Cards)
- **📄 Footer Sections**: Footer variants 2 แบบ (Basic, Mega)
- **📖 About Sections**: About variant (Basic)
- **🍽️ Menu/Product Sections**: Menu variants 2 แบบ (Basic, Carousel)

### 2. **Business Category Selector** 🏢

เลือก Business Category เพื่อใช้ Color Palette และ Tone:

- Restaurant, Cafe, Bakery, Bar, Salon, Spa, Gym, Clinic

### 3. **Variant Selector** 🎭

เลือก Variant ที่ต้องการดู แต่ละ Block Type มี Variants แตกต่างกัน

**Compare Mode**: เมื่อเปิด Compare Mode จะมี checkbox ให้เลือกหลาย Variants เพื่อเปรียบเทียบ

### 4. **Viewport Selector** 📱

ดูแบบหน้าจอต่างๆ:

- **📱 Mobile**: 375px
- **📲 Tablet**: 768px  
- **🖥️ Desktop**: 1440px
- **📺 Full Width**: 100%

### 5. **Preview Mode** 🖥️💻

- **🖥️ Live**: แสดงผล HTML จริง
- **💻 Code**: แสดง Template Source Code

### 6. **Mock Data Editor** ✏️

คลิกปุ่ม **✏️ Edit Data** เพื่อเปิด Sidebar:

- แก้ไขข้อมูล Mock Data แบบ Real-time
- รองรับ Text Input และ Textarea
- ปุ่ม **Reset** เพื่อคืนค่าเริ่มต้น

**ตัวอย่างข้อมูลที่แก้ไขได้:**
- heading, subheading, badge
- ctaLabel, secondaryCta
- stat1, stat2, stat3 (สำหรับ Hero Stats)
- heroImage, heroImageAlt
- และอื่นๆ ตาม Block Type

### 7. **Compare Mode** ⚖️

คลิกปุ่ม **⚖️ Compare** เพื่อเปิดโหมดเปรียบเทียบ:

1. เปิด Compare Mode
2. เลือก Variants ที่ต้องการเปรียบเทียบ (checkbox จะปรากฏ)
3. ดูแบบ Side-by-Side (2 คอลัมน์)

### 8. **Export Code** 📋

คลิกปุ่ม **📋 Copy Code** เพื่อคัดลอก Template Code ไปยัง Clipboard

---

## 🔧 วิธีการใช้งาน

### ขั้นตอนพื้นฐาน

1. **เลือก Block Type** ที่ต้องการทดสอบ (เช่น Hero Sections)
2. **เลือก Business Category** (เช่น Restaurant)
3. **เลือก Variant** (เช่น Hero Stats)
4. **ดูผลลัพธ์** ใน Preview Area

### การปรับแต่งข้อมูล

1. คลิก **✏️ Edit Data**
2. แก้ไขข้อมูลใน Sidebar
3. ดูผลลัพธ์เปลี่ยนแปลงทันที

### การดู Responsive

1. คลิกเลือก Viewport ที่ต้องการ (📱📲🖥️📺)
2. Preview จะปรับขนาดตาม Viewport

### การเปรียบเทียบ Variants

1. คลิก **⚖️ Compare**
2. เลือก Variants ที่ต้องการเปรียบเทียบ (เช็ค checkbox)
3. ดูผลเปรียบเทียบแบบ Side-by-Side

### การ Export Code

1. เลือก Variant ที่ต้องการ
2. คลิก **📋 Copy Code**
3. Paste ไปใช้ในโปรเจ็กต์

---

## 🎨 Color Palette System

ระบบใหม่แก้ไขปัญหา Tailwind Dynamic Classes:

### ปัญหาเดิม ❌
```tsx
// ไม่ทำงาน - Tailwind ไม่รู้จัก class ที่สร้างแบบ dynamic
<div className={`bg-${primary}-500`}>
```

### วิธีแก้ไข ✅
```tsx
// ใช้ Color Mapper แปลงเป็น inline styles
import { getColorValue } from './ColorMapper';

const bgColor = getColorValue('blue', 500); // '#3b82f6'
<div style={{ backgroundColor: bgColor }}>
```

### รองรับสีทั้งหมด
- blue, green, red, yellow, purple, pink, orange, teal, indigo, gray

---

## 📁 ไฟล์ที่เกี่ยวข้อง

```
Midori/
├── src/
│   ├── app/(app)/layout-tester/
│   │   └── page.tsx                    # หน้าหลัก Layout Tester
│   └── components/layout-tester/
│       ├── VariantPreview.tsx          # Component สำหรับแสดง Preview
│       ├── TemplateRenderer.tsx        # Renderer + Color Conversion
│       ├── ColorMapper.ts              # Color Palette Mapper
│       └── BlockTypeConfig.ts          # Block Type Configuration
└── midori/agents/frontend-v2/template-system/
    └── shared-blocks/variants/
        ├── hero-variants.ts
        ├── footer-variants.ts
        ├── about-variants.ts
        └── menu-variants.ts
```

---

## 🆕 สิ่งที่เพิ่มเข้ามา

### Phase 1: แก้ไขปัญหาเร่งด่วน ✅
- ✅ แก้ไข Tailwind dynamic classes
- ✅ เพิ่มรองรับ variants ทุกประเภท

### Phase 2: ปรับปรุง UX ✅
- ✅ Responsive preview modes
- ✅ Mock data editor  
- ✅ UI ใหม่พร้อม sidebar

### Phase 3: ฟีเจอร์ขั้นสูง ✅
- ✅ Compare mode
- ✅ Export code
- ⚠️ Screenshot (ใช้ Copy Code แทน - สามารถเพิ่ม html2canvas ภายหลังได้)

---

## 💡 Tips & Tricks

### 1. การทดสอบสีต่างๆ
- เปลี่ยน Business Category เพื่อดูสีต่างๆ
- Restaurant = Orange, Cafe = yellow, Gym = red, Spa = purple

### 2. การหา Variant ที่เหมาะสม
- ใช้ Compare Mode เปรียบเทียบหลาย Variants
- ทดสอบกับ Category ต่างๆ

### 3. การ Debug Template
- เปลี่ยนเป็น Code Mode เพื่อดู Template Source
- ตรวจสอบว่า placeholders ถูกแทนที่หรือไม่

### 4. การปรับแต่งข้อมูล
- ใช้ Mock Data Editor ทดสอบกับข้อมูลจริง
- ลองใส่ข้อความยาว/สั้นเพื่อดูการ render

---

## 🐛 การแก้ปัญหา

### Preview ไม่แสดงผล
- ตรวจสอบว่า Block Type และ Variant ถูกเลือกหรือไม่
- ลอง Reset Mock Data

### สีไม่ถูกต้อง
- ตรวจสอบ Business Category ที่เลือก
- ระบบจะใช้ inline styles แทน Tailwind classes

### Template Error
- ดู Console สำหรับ error messages
- ลองเปลี่ยนเป็น Code Mode ดู template source

---

## 🚀 การพัฒนาต่อ (Optional)

### เพิ่ม Screenshot Feature
```bash
npm install html2canvas
```

```tsx
import html2canvas from 'html2canvas';

const exportAsImage = async () => {
  const element = document.getElementById('preview-container');
  const canvas = await html2canvas(element);
  const link = document.createElement('a');
  link.download = `variant-${selectedVariant.id}.png`;
  link.href = canvas.toDataURL();
  link.click();
};
```

### เพิ่ม Favorites System
- บันทึก Variant + Mock Data ที่ชอบ
- ใช้ localStorage เก็บข้อมูล

---

## 📞 สนับสนุน

หากมีปัญหาหรือข้อเสนอแนะ กรุณาติดต่อทีมพัฒนา

**Happy Testing! 🎉**

