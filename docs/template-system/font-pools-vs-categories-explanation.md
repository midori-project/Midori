# 📖 ความแตกต่าง: Typography vs Font Pools

## ❓ คำถาม
> "ส่วนของตรงนี้ต่างกันอย่างไร และมีหน้าที่อย่างไร"

## 🎯 คำตอบแบบสั้นๆ

**2 ส่วนนี้ทำงานร่วมกัน แต่มีหน้าที่ต่างกัน:**

1. **`restaurants.ts` (Typography)** = **"ฟอนต์ที่ใช้จริง"** (Actual Font)
2. **`index.ts` (CATEGORY_FONT_POOLS)** = **"ฟอนต์ที่ใช้ได้"** (Allowed Fonts)

---

## 📊 เปรียบเทียบ

| คุณสมบัติ | Typography (restaurants.ts) | Font Pools (index.ts) |
|-----------|---------------------------|----------------------|
| **ที่อยู่** | ในแต่ละ category file | รวมไว้ที่ index.ts |
| **ค่าที่เก็บ** | Font ที่เลือกใช้จริง | รายการ font ที่ใช้ได้ |
| **จำนวน** | 1 font ต่อ category | หลาย font ต่อ category |
| **ใช้เมื่อไหร่** | ส่งให้ AI / Render | ตรวจสอบ validation |
| **เปลี่ยนแปลง** | เปลี่ยนได้ตามต้องการ | เปลี่ยนยาก (hardcoded) |
| **ตัวอย่าง** | Poppins (fixed) | [playfair, crimson, poppins, ...] |

---

## 🔄 การทำงานร่วมกัน

### Flow 1: การใช้งานปกติ

```
1. User สร้าง restaurant website
   ↓
2. ใช้ Font Pool ตรวจสอบ
   → allowedFonts: ['playfair', 'crimson', 'poppins', ...]
   → ตรวจว่า font ที่ใช้ถูกต้องหรือไม่
   ↓
3. ใช้ Typography จาก category
   → fontFamily: 'Poppins' (จริง)
   → ส่งให้ AI + Render
   ↓
4. ได้เว็บไซต์ที่มีฟอนต์ Poppins
```

### Flow 2: การเปลี่ยนฟอนต์

```typescript
// สมมุติ AI ต้องการฟอนต์ 'playfair' แทน

// ✅ ขั้นตอน 1: ตรวจสอบใน Font Pool
isFontAllowed('restaurant', 'playfair') 
→ true ✅ (เพราะ playfair อยู่ใน allowedFonts)

// ✅ ขั้นตอน 2: ใช้งานได้
→ Typography เปลี่ยนจาก 'Poppins' เป็น 'playfair'
→ Render ด้วย Playfair Display
→ ได้เว็บไซต์ที่มีฟอนต์ Playfair Display
```

---

## 🎨 ตัวอย่างการใช้งาน

### ตัวอย่าง 1: Restaurant Category

```typescript
// 📁 restaurants.ts
globalSettings: {
  typography: {
    fontFamily: 'Poppins',  // ⭐ ฟอนต์ที่ใช้จริง
    googleFont: 'Poppins:wght@300;400;600;700',
    fallback: ['sans-serif']
  }
}
```

```typescript
// 📁 index.ts
CATEGORY_FONT_POOLS: {
  'restaurant': {
    allowedFonts: [          // ⭐ ฟอนต์ที่ใช้ได้
      'playfair', 
      'crimson', 
      'poppins',   ← ตรงกับที่ใช้จริง
      'nunito', 
      'inter'
    ],
    defaultFont: 'poppins',  // Default ถ้าไม่มีระบุ
    randomSelection: true    // สุ่มได้
  }
}
```

### ตัวอย่าง 2: การเปลี่ยนฟอนต์

**ก่อน:**
```json
"typography": {
  "fontFamily": "Poppins"
}
```
Result: ใช้ Poppins

**หลัง:**
```json
"typography": {
  "fontFamily": "playfair"  // เปลี่ยนเป็น Playfair
}
```
Result: ใช้ Playfair Display

**เงื่อนไข:** ต้องอยู่ใน allowedFonts เท่านั้น!

---

## ⚠️ กรณีผิดปกติ

### กรณีที่ Font Pool ป้องกันไว้

```typescript
// ❌ ใช้ฟอนต์ที่ไม่อยู่ใน pool
typography: {
  fontFamily: 'Lora'  // สำหรับ news เท่านั้น
}

// ระบบจะ:
1. ตรวจสอบ: isFontAllowed('restaurant', 'lora')
2. ได้: false ❌
3. Action: ใช้ default font (Poppins) แทน
```

---

## 💡 เหตุผลที่มี 2 ส่วน

### ทำไมถึงแยกกัน?

| ข้อดี | Typography | Font Pools |
|------|-----------|------------|
| ความยืดหยุ่น | ✅ เปลี่ยนได้ง่าย | ❌ Hardcoded |
| ควบคุม | ❌ ใช้งานเดียว | ✅ กำหนดได้หลายทางเลือก |
| ความปลอดภัย | ❌ ไม่มี validation | ✅ มี validation |
| Backend/Frontend | ✅ Frontend | ✅ Backend logic |

### ตัวอย่างการทำงานจริง

```typescript
// การทำงานจริงเมื่อ render

// Step 1: AI เลือกฟอนต์
const selectedFont = 'playfair';

// Step 2: ตรวจสอบ Font Pool
if (isFontAllowed('restaurant', selectedFont)) {  // ✅ true
  // Step 3: ใช้ Typography
  typography.fontFamily = selectedFont;  // "Playfair Display"
} else {
  // Step 4: ใช้ default
  typography.fontFamily = getDefaultFont('restaurant');  // "Poppins"
}

// Step 5: Render
const css = `
  body {
    font-family: ${typography.fontFamily}, ${typography.fallback};
  }
`;
// Result: font-family: Playfair Display, serif;
```

---

## 🎯 สรุป

### Typography (restaurants.ts)
- ✅ **คืออะไร**: ค่าฟอนต์ที่ใช้จริง
- ✅ **หน้าที่**: กำหนด font ใน website
- ✅ **ใช้เมื่อ**: Render, ส่งให้ AI
- ✅ **เปลี่ยนได้**: เปลี่ยนได้ง่าย

### Font Pools (index.ts)
- ✅ **คืออะไร**: รายการฟอนต์ที่ใช้ได้
- ✅ **หน้าที่**: Validation + ควบคุม
- ✅ **ใช้เมื่อ**: ตรวจสอบความเป็นไปได้
- ✅ **เปลี่ยนได้**: เปลี่ยนยาก (ต้องแก้ code)

---

## 📋 ตารางสรุป

| คำถาม | Typography | Font Pools |
|--------|-----------|------------|
| เปลี่ยนแปลงได้หรือไม่? | ✅ ง่ายมาก | ❌ ยาก (hardcoded) |
| มีกี่ค่า? | 1 | หลายค่า |
| ชุดไหนบังคับ? | ไม่บังคับ | บังคับ (constraints) |
| ใช้เมื่อไหร่? | เสมอ | เมื่อต้อง validation |
| ใครเป็นคนตั้ง? | AI/System | Developer |

---

## ✨ ตัวอย่างการใช้งานจริง

### Restaurant Website ที่สร้างขึ้น

```css
/* CSS ที่ได้ */
<link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&display=swap" rel="stylesheet">

body {
  font-family: Poppins, sans-serif;  /* ⬅️ มาจาก Typography */
}
```

**ทำไมถึงเป็น Poppins?**
1. ✅ อยู่ใน Font Pool (allowedFonts)
2. ✅ เป็น default font
3. ✅ ผ่าน validation
4. ✅ ถูกใช้จริง (Typography)

---

## 🎓 สรุปสั้นๆ

```
Typography = "ฟอนต์ที่ใช้จริง" (Actual)
Font Pools = "ฟอนต์ที่ใช้ได้" (Allowed)
```

ทั้งสองทำงานร่วมกัน:
- Font Pools = กติกา (Rules)
- Typography = การใช้งาน (Usage)

🗣️ พูดง่ายๆ: Font Pools บอก "ใช้ได้แค่ไหน" และ Typography บอก "ใช้ตัวไหน"

